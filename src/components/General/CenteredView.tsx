import styled from 'styled-components/native';
import { Container } from 'native-base';

const CenteredView = styled(Container)`
  justify-content: center;
  align-items: center;
`;

export default CenteredView;
